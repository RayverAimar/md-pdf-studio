# electron-builder cannot traverse pnpm's symlinked node_modules when collecting the app's
# production dependencies; a hoisted layout for this app makes packaging resolve them correctly.
node-linker=hoisted
